[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "bran-agent-skills-orchestrator"
version = "0.1.0"
description = "Conservative, local AI development orchestration primitives"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
bran-agent-orchestrate = "orchestrator.cli:main"

[tool.setuptools.packages.find]
include = ["orchestrator*"]
