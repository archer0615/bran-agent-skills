# Skill authoring

Keep each skill narrow and operational. Use YAML frontmatter with `name` and a specific `description`, followed by `Use when`, `Procedure`, `Verification`, and `Output` sections. Do not include secrets, personal data, or machine-specific paths.
